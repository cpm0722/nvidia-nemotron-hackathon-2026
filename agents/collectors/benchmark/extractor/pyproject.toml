[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=64"]

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "nat_extractor_benchmark"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
description = "NAT A2A benchmark extractor — scrape + validator A2A call"
keywords = ["ai", "agents", "scraper", "benchmark", "nat", "a2a", "extractor"]
classifiers = ["Programming Language :: Python"]
dependencies = [
    "nvidia-nat[a2a]",
    "ari_core",
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
]

[tool.uv.sources]
ari_core = { path = "../../../../libs/ari-core", editable = true }

[project.optional-dependencies]
test = ["pytest>=8.0"]

[project.entry-points."nat.components"]
nat_extractor_benchmark = "nat_extractor_benchmark.register"
